// import { Request } from 'express';
// import { TokenService } from './token.service'; // Adjust the path as necessary

// export async function CheckToken(
//   request: Request,
//   tokenService: TokenService,
// ): Promise<boolean> {
//   const tokenId = request.token; // or another header or query parameter where token ID is sent

//   if (!tokenId) {
//     throw new Error('Token ID is missing');
//   }

//   return tokenService.isValidToken(tokenId);
// }


